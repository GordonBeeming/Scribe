name: Scribe
options:
  bundleIdPrefix: com.gordonbeeming
  deploymentTarget:
    iOS: "26.0"
    watchOS: "26.0"
  xcodeVersion: "26.2"
  generateEmptyDirectories: true
  groupSortPosition: top

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    DEVELOPMENT_TEAM: 89DBN36T4T

targets:
  Scribe:
    type: application
    platform: iOS
    configFiles:
      Release: ci-signing-app.xcconfig
    sources:
      - Scribe
    resources:
      - path: Scribe/PrivacyInfo.xcprivacy
    info:
      path: Scribe/Info.plist
      properties:
        UIBackgroundModes:
          - remote-notification
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.gordonbeeming.scribe
        CODE_SIGN_ENTITLEMENTS: Scribe/Scribe.entitlements
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: 1
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: true
        TARGETED_DEVICE_FAMILY: "1,2"
    dependencies:
      - target: ScribeWidgetExtension
        embed: true

  ScribeWidgetExtension:
    type: app-extension
    platform: iOS
    configFiles:
      Release: ci-signing-widget.xcconfig
    sources:
      - path: ScribeWidget
      - path: Scribe/Utilities
        group: Shared/Utilities
      - path: Scribe/Models
        group: Shared/Models
    info:
      path: ScribeWidget/Info.plist
      properties:
        CFBundleDisplayName: Scribe Widget
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
        EXAppExtensionAttributes:
          EXExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.gordonbeeming.scribe.widget
        CODE_SIGN_ENTITLEMENTS: ScribeWidget/ScribeWidget.entitlements
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: 1
        TARGETED_DEVICE_FAMILY: "1,2"

  ScribeWatch:
    type: application
    platform: watchOS
    sources:
      - path: ScribeWatch
      - path: Scribe/Utilities
        group: Shared/Utilities
      - path: Scribe/Models
        group: Shared/Models
      - path: Scribe/CloudKit
        group: Shared/CloudKit
      - path: Scribe/Services
        group: Shared/Services
    info:
      path: ScribeWatch/Info.plist
      properties:
        UIBackgroundModes:
          - remote-notification
        WKApplication: true
        ITSAppUsesNonExemptEncryption: false
        WKCompanionAppBundleIdentifier: com.gordonbeeming.scribe
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.gordonbeeming.scribe.watch
        CODE_SIGN_ENTITLEMENTS: ScribeWatch/ScribeWatch.entitlements
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: 1
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    dependencies:
      - target: ScribeWatchWidgetExtension
        embed: true

  ScribeWatchWidgetExtension:
    type: app-extension
    platform: watchOS
    sources:
      - path: ScribeWatchWidget
      - path: Scribe/Utilities
        group: Shared/Utilities
      - path: Scribe/Models
        group: Shared/Models
    info:
      path: ScribeWatchWidget/Info.plist
      properties:
        CFBundleDisplayName: Scribe Watch Widget
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.gordonbeeming.scribe.watch.widget
        CODE_SIGN_ENTITLEMENTS: ScribeWatchWidget/ScribeWatchWidget.entitlements
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: 1

  ScribeTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - ScribeTests
    dependencies:
      - target: Scribe
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.gordonbeeming.scribe.tests
        GENERATE_INFOPLIST_FILE: true
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Scribe.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Scribe"
        BUNDLE_LOADER: "$(TEST_HOST)"

schemes:
  Scribe:
    build:
      targets:
        Scribe: all
        ScribeTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ScribeTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

  ScribeWatch:
    build:
      targets:
        ScribeWatch: all
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
